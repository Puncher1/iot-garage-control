export enum ErrorType {
    offline = 0,
    connection = 1,
    sending = 2,
}

export enum GateControlOption {
    open = 0,
    paused = 1,
    closed = 2,
}

export enum AirControlOption {
    percent_0 = 0,
    percent_25 = 1,
    percent_50 = 2,
    percent_75 = 3,
    percent_100 = 4
}
